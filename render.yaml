services:
  - type: web
    name: gymfreak-app
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: cd server && node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /api/profile
